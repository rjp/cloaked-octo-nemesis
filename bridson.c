/* Generate a Poisson Disc using Bridson */
#include <stdio.h>
#include <stdlib.h>

/* We need to keep track of active vs inactive points */
typedef struct { int x; int y; } point;

void
generate_samples(int **output, int width, int height, int annulus)
{
}
